export type WindowType = {
    id: number,
    title: string,
    component?: {},
    html?: string,
    minimized: boolean,
}

export type CreationWindowType = {
    title: string,
    component?: {},
    html?: string,
}